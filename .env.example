# Cloudflare 的 Deploy to Cloudflare 會讀取此檔，並把每個 KEY 顯示成部署表單欄位。
# 本課程的 Worker 不在部署時收任何憑證：部署完成後，請開啟 /join，親手填入自己的
# LINE Channel secret 與 access token。這樣憑證不會混在平台設定畫面，也能理解 webhook 接線。
#
# 需要在本機啟動舊版 Node 範例時，複製 docs/local-env.example 成 .env；不要把 .env 提交到 GitHub。
